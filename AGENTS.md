# Agent Instructions

- After each resolved issue, create a git commit with a descriptive message.
- Do not ask for confirmation before committing.
- Do not push commits unless the user explicitly confirms; commit locally only.
